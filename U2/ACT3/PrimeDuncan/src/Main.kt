/**
 * Pedir un número al usuario para comprobar si el número es primo o no.
 * Luego preguntaremos si quiere comprobar otro número.
 *
 * @author Duncan Rua Valiente
 * @version 1.1
 *
 */
fun main(args: Array<String>) {
    /**
    do {
        println("Dime un numero")
        var number = readln().toInt()

        if (number %2 == 0){
            println("El número es primo")
        }else{
            println("El número no es primo")
        }

        print("""|
                 |Quieres comprobar otro numero?
                 |Si o No?""".trimMargin())
        var answer = readln()
        var ans: String = "Hola"
        var answerUpperCase = ans.uppercase()

        if ()
    }while (number != 0)
    */
    var st = "hola"
    var upperSt = st.uppercase()

    println(upperSt)
}
/**
 * Describe una descripción
 * @author: Duncan Rua Valiente
 * @version: 1.0
 */

fun main(args: Array<String>) {
    /**
    //Comprobación de que solo tiene letras y tiene mas de 3 y menos que 50
    val checkName = Regex("[A-z]{3,50}")
    val checkEmail = Regex
    val checkDni = Regex
    val checkAge = Regex
    val checkTelephone = Regex

    //Pedir nombre
    println("Dime tu nombre completo")
    val fullname = readln()
    if (fullname.matches(checkName)){
        println(fullname)
    }else{
        println("Dato invalido")
    }
    **/
    val num = 0..10
    println(num)    
}